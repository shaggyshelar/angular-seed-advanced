export class Ticket {
    constructor(
        id,
        ticket,
        department,
        concern,
        description,
        status,
        priority,
        createdDate,
        updatedDate,
        updatedBy,
        resolvedBy,
        ageDays,
    ) { }
}
